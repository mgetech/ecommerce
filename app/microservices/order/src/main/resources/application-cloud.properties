spring.application.name=order
server.port=8080

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

user.service.url=${USER_SERVICE_URL:https://user-service-716746262210.europe-west3.run.app}
product.service.url=${PRODUCT_SERVICE_URL:https://product-service-716746262210.europe-west3.run.app}

# Let Spring retry on startup and wait for the db
#spring.sql.init.mode=never
spring.datasource.hikari.initialization-fail-timeout=-1

spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.enabled=false
spring.kafka.listener.enabled=false